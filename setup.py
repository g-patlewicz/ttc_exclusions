from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Investigation of the feasibility of refining exclusion rules within the Kroes et al (2004) workflow for TTC.',
    author='patlewig',
    license='MIT',
)
